<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/mellanBla</item>
        <item name="colorPrimaryDark">@color/morkBla</item>
        <item name="colorAccent">@color/ljusBla</item>
    </style>

</resources>
